spring:
  application:
    name: chat-gpt-telegram-bot
  profiles:
    active: dev
    default: dev

server:
  port: 8080

bot:
  key: zero
  name: zero
  owner-chatId: zero

openai:
  api:
    key: key
    chat-url: https:/api.openapi.com/v1/chat/completions
    chat-model: gpt-4o
    chat-system-role: rolla
    transcription-url: transa
    voice-model: modla
    language: langa
